import membershipConfig from '../../../shared/membershipConfig.json';

export const DEFAULT_MEMBERSHIP = membershipConfig.defaultMembership;
export const TIER_CREDITS = membershipConfig.TIER_CREDITS;
export const SUBSCRIPTION_PRICES = membershipConfig.SUBSCRIPTION_PRICES;
export const TIER_FEATURES = membershipConfig.TIER_FEATURES;
export const TIER_ORDER = membershipConfig.TIER_ORDER; 